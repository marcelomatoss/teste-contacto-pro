global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: contactpro-bot

scrape_configs:
  - job_name: contactpro-backend
    metrics_path: /metrics
    static_configs:
      - targets:
          - backend:3001
